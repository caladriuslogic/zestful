//! `zestful top` — live agent TUI. Network-only client of the daemon's
//! HTTP+SSE API. See spec: docs/superpowers/specs/2026-04-29-zestful-top-tui-design.md.

use anyhow::Result;

pub fn run() -> Result<()> {
    eprintln!("zestful top: not yet implemented");
    Ok(())
}
